<%= form_for(@patient, :html => { :id => '.form-horizontal' }) do |f| %>
  <% if @patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
      <% @patient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <div class="controls">
        <%= f.text_field :name %>
    </div>
  </div>
  <div class="field">
    <%= f.label :birth %><br>
    <div class="controls">
        <%= f.date_field :birth %>
    </div>
  </div>

  <p><%= link_to 'New User', new_patient_user_path(@patient) %></p>

  <p><%= link_to 'New Exam', new_patient_exam_path(@patient) %></p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

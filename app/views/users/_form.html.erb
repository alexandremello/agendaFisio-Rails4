<%= form_for(@user, :html => { :id => '.form-horizontal' }) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <div class="controls">
        <%= f.text_field :name %>
    </div>
  </div>
  <div class="field">
    <%= f.label :birth %><br>
    <div class="controls">
        <%= f.date_field :birth %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
